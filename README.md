# Conferences

Collection of my conference material.

## Building

These presentations use [Marp](https://marp.app/) for its slides:

```console
pnpm -g install @marp-team/marp-cli
```

You can serve it as such:

```console
marp --html -w -s .
```
